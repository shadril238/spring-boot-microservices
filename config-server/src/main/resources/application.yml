spring:
    application:
        name: "config-server"
    profiles:
        active: native
    cloud:
        config:
            server:
                native:
#                    searchLocations: "classpath:/config"
                    searchLocations: "file:///home//bs00545//Desktop//spring-boot-microservices//config"  # Path to the directory where the configuration files are stored
server:
    port: 8071